[x] delete the dead reqwest dependency from clearhead-cli $ reqwest was declared with zero usages anywhere in the crate; it alone pulled native-tls -> openssl and the hyper/h2 stack. removed the line; cargo tree -i openssl now matches nothing, full cli suite green (bar the pre-existing linter snapshot below). eliminates all 8 openssl alerts. also a prerequisite for cross-compiling release binaries — openssl is the classic cross-compile blocker $ !1 +cli,security %2026-07-03T12:00#019f290e-669c-7027-bc60-f5a3094a3cb5
[x] swap serde_yml for a maintained yaml crate in clearhead-core ~$ replaced serde_yml (unsound) with serde_yaml_ng 0.10 (API-compatible drop-in) at the two charter.rs frontmatter call sites; cargo tree confirms serde_yml and libyml are both gone from the lockfile, core suite green (219+ tests) $ !2 +core,security %2026-07-03T12:00#019f290e-669c-7202-94a9-fc426f79b5ed
    >[x] replace the two serde_yml call sites and the Cargo.toml line  %2026-07-03T12:00#019f290e-669c-7884-9c2a-545a8f43cd75
    >[x] confirm serde_yml and libyml are gone from the lockfile  %2026-07-03T12:00#019f290e-669c-7f05-94ff-40e5048c9686
[x] refresh lockfiles across the three dependency trees ~$ cargo update in core and cli (bytes 1.11->1.12 patches the overflow advisory, rand 0.9.2->0.9.4, time 0.3.47->0.3.53, automerge 0.7.3->0.7.4); npm update in tree-sitter-actions took npm audit to "found 0 vulnerabilities" (fast-uri x2 + tar-fs cleared, all were devDependency-only) $ !2 +security %2026-07-03T12:00#019f290e-669c-721a-b6ce-790a71947db0
    >[x] cargo update in clearhead-core and clearhead-cli  %2026-07-03T12:00#019f290e-669c-720a-addd-73f15a07e950
    >[x] npm update in tree-sitter-actions and merge the dependabot PRs  %2026-07-03T12:00#019f290e-669c-7fc2-bb76-f47645b18c11
[x] fix the real "jsonld tests failing" — stale json output contract in clearhead-cli ~$ the actual failing tests were test_read_acts_json_format and test_json_output_validates_against_schema, not the CI. read acts moved from a plain JSON array (orphaned to_value path in lib.rs:94, now confirmed dead for core-seam to sweep) to a JSON-LD document, and the OutputMode enum renamed the value to json-ld, but the tests still passed the old value json and asserted an array. CONFIRMED: tests follow the code to the single JSON-LD contract; json stays as an alias for jq ergonomics. principle agreed — one semantic output contract, alternate shapes (e.g. a --flatten/--no-context view dropping @context to emit the bare @graph array) are config views on that one pipeline, never a parallel serializer. the flatten view is deferred (YAGNI) until real scripting friction appears $ !1 +cli,decision %2026-07-03T12:00#019f290e-669c-7752-a402-081b07afb4ab
    >[ ] add a --flatten/--no-context view on the json-ld output IF scripting friction appears $ drops the @context envelope and emits the bare @graph node array; same serializer, same vocab names, just less envelope. do NOT build speculatively — wait for a real consumer need $ +cli,someday #019f290e-669c-7703-807c-81b152e034a5
    >[x] add #[value(alias = "json")] to OutputMode::JsonLd so --format json still resolves  %2026-07-03T12:00#019f290e-669c-77a8-add9-2bb9366f44ac
    >[x] rewrite both tests to assert the JSON-LD document shape (@graph array, contains title)  %2026-07-03T12:00#019f290e-669c-7ee5-b7b9-07cced1236e1
[x] pause clearhead-core CI pending the release-pipeline rethink $ per your call — the standalone build cannot succeed while tree-sitter-actions is a super-repo-only sibling path dep, and we would rather solve that inside a real release pipeline than patch it twice. switched ci.yml auto-triggers to workflow_dispatch only, with a comment pointing here. reversible $ !1 +core,ci %2026-07-03T12:00#019f290e-669c-7cdd-9bc7-11e1f1c8258d
[ ] design the GH-releases + cargo binstall pipeline $ the replacement for standalone CI: build and publish prebuilt clearhead binaries to GH Releases so low-power devices cargo binstall without paying Rust build times. this is where the tree-sitter-actions dependency form finally gets settled — a release build can run from the super-repo checkout where the sibling exists, or we pin a git/registry version. needs: target matrix (removing openssl already unblocked clean cross-compile), [package.metadata.binstall] naming in Cargo.toml, a release-tag-triggered workflow, and a decision on where the build runs (super-repo vs standalone) $ !2 +ci,cli,release #019f290e-669c-773d-b80d-4af0cbcb80db
[ ] investigate the pre-existing E003 linter snapshot failure $ NOT caused by this cleanup — test_linting_error_detection fails independently of every change here (survived reqwest removal AND cargo update). the empty-context-tag diagnostic range end_col shifted 37 -> 34, i.e. the committed snapshot is stale against the current tree-sitter-actions grammar pin (same submodule-drift theme as the CI break). decide whether 34 is correct (accept snapshot) or a grammar regression before running cargo insta accept $ !3 +cli,bug #019f290e-669c-79f7-8223-b7113b161f0c