[ ] Route the add verb through the locked core seam =durable-add $Add a pure plan_insert_action and route add through with_locked_mutation: lock, recover, read trusted state, compute the insertion-after-subtree index, stage the re-rendered active file plus its sidecar, commit one PendingBatch. Dry-run is the pure plan on an unlocked read. The CLI keeps flag-to-Action assembly and dry-run prose; the raw read_actions/insert/save_file path is removed.$ !1 +architecture ^2026-08-15T12:24 <locked-mutation-envelope #01a006e1-c0dc-78c1-b449-7c1a83afe965
[ ] Route the update verb through the locked core seam =durable-update $Add a pure plan_update_action and route update through with_locked_mutation using the relocated ActionUpdate. Move the completed_at stamping and the in-place terminal-state guard into core so the rule has one home. Record an explicit decision on the relationship to the data-workflows transact planner (reuse, share, or keep separate) rather than silently duplicating it.$ !1 +architecture ^2026-08-15T12:24 <durable-add #01a006e1-c626-7b91-b500-054f254f1ee6
[ ] Generalize the action selector across active and completed =generalize-selector $Generalize CloseActionSelector (active-only) into a selector that spans active and .completed.actions (delete targets both) and carries typed under-lock outcomes (not-found-under-lock, state-changed-under-lock) analogous to close's already_closed. Migrate close_action_subtree onto the generalized selector with no behavior change so the durable-action-close re-identification rule applies uniformly to close, update, and delete.$ !1 +architecture ^2026-08-15T12:24 <durable-update #01a006e1-cb7b-73a3-832e-c782c5d4ed37
[ ] Route the delete verb through the locked core seam =durable-delete $Add a pure plan_delete_action_subtree and route delete through with_locked_mutation for the open-subtree case and the completed-archive single-remove case. The CLI keeps its workspace fan-out and file selection; the raw retain/save_file path is removed.$ !1 +architecture ^2026-08-15T12:24 <generalize-selector #01a006e1-d045-7f73-9013-3cf4c4e15a97
[ ] Prove interrupted verbs recover and close the charter =durable-verbs-close $Add crash-point tests proving interrupted add/update/delete recover through recover_pending with no lost or duplicated bytes. Rerun the CLI acceptance suite and the fuzzy-fallback resolution_tests unchanged, then scripts/validate-pinned against the exact pinned composition. Review the done gate and close.$ !2 +architecture ^2026-08-15T12:24 <durable-delete #01a006e1-d69b-7181-8117-66e0b45bd942
