[x] delegate save_file to core write_actions $commands/mod.rs save_file duplicates core action_files::write_actions body-for-body (same format call) minus the atomic rename; replace its body with write_actions plus the sidecar stamp$ +cli %2026-07-03T09:02 #019f268f-eb33-7c70-b9e2-d79f46223f3d
[x] sweep the remaining naive workspace-data writes $plan.rs writes .ics with fs::write, charter.rs writes charter markdown in three places, archive.rs writes the completed file directly — route each through durability::atomic_write. exempt: write_or_print user-directed output, init.rs one-time bootstrap, gen_man. SEQUENCED after the caldav reconcile shell because its step 1 lifts the plan.rs mirror-path logic into core — sweeping plan.rs first would collide with that move$ +cli %2026-07-11T14:05 <019f12d4-4f7c-7bc2-9c76-b048067d43e8 #019f268f-eb35-7c28-9704-bfb7efbd714d
[x] finish routing CLI writes through the durability primitive ~ $write-durability closed with core's write sites converted, but the CLI's own save paths still call naive std::fs::write — every complete, update, cancel, and delete goes through commands/mod.rs save_file, so a crash mid-write can truncate a source-of-truth file. the crash-safety layer exists and is tested; the most common writes bypass it$ !1 +cli %2026-07-11T14:22 #019f268f-eb32-747c-aa98-37eabca59a11
[x] unify action reference resolution on the core resolver ~ $three implementations exist — core reference::resolve_reference is the real one, mutations::resolve_reference is a flat-list copy, and act_matches in commands/action.rs is a boolean check used by all mutating commands. act_matches breaks the spec's resolution order, an earlier name-contains match shadows a later exact alias, so complete can close the wrong action. NOTE 2026-07-11: this item and its three children were marked complete once already with no matching code or commit anywhere in the repo — caught during charter review and fixed for real this pass; see child descriptions for what actually landed$ !1 +cli,bug %2026-07-11T14:50 #019f268f-eb37-7ef1-acb7-dc303a66d7ae
    >[x] fix alias-beats-name precedence in mutating commands $this is the user-facing bug and lands first — find_act_mut must prefer full uuid, then short uuid, then alias, then name-contains across the whole list rather than first-match-any per action. landed as action_match_tier + find_best_match/_mut/_pos in commands/action.rs, replacing the OR'd boolean action_matches for every call site that resolves identity (find_action_mut, show_action, delete_action); action_matches itself survives only as an existence-only check where identity doesn't matter. regression tests: commands::action::resolution_tests$ +bug %2026-07-11T14:50 #019f268f-eb38-7614-b246-1d7d44c49322
    >[x] delete act_matches and the mutations resolver, delegate to one implementation ~ $complete, update, cancel, and delete resolve through the same code path as plan.rs and the LSP; the ActionList-level entry can live in core beside reference.rs if the DomainModel shape is too heavy for file-local commands. LANDED DIFFERENTLY: core's reference::resolve_reference deliberately excludes name-contains matching for actions (see its own doc comment) and cannot serve mutating commands as-is, so there was nothing to delegate to. What actually happened: mutations::resolve_reference/MatchType/ResolvedAction were dead code once find_plan_file/find_plan_file_for_mutation were deleted (their only callers) and got deleted with them; the real fix — precedence order — landed in place at commands/action.rs, the actual seam every mutating command goes through, not in core$ +cli,library %2026-07-11T14:50 #019f268f-eb3a-7e79-b865-ac4c25dcba48
    >[x] port the alias-before-name regression test to the surviving seam $mutations.rs test_resolve_alias_before_name is the guard that documents the spec order — it must outlive the module it currently lives in. lands as commands::action::resolution_tests::alias_beats_earlier_name_contains_match, alongside two new tests for the uuid/short-uuid tiers the original test didn't cover$ +testing %2026-07-11T14:50 #019f268f-eb3b-7adb-8fc6-1161be233e32
[x] deduplicate workspace config between CLI and core $environment_reader::Config carries get_tag_ancestors, expand_tag, and expand_tags as character-for-character copies of WorkspaceConfig in core config.rs, including roughly ninety lines of duplicated tests — and core's copy is strictly better because it also has descendants_and_self$ !2 +cli %2026-07-11T14:27 #019f268f-eb3f-7238-928f-7fcb123ba985
    >[x] delete the dead tag-hierarchy methods and their copied tests $get_tag_ancestors, expand_tag, and expand_tags on environment_reader::Config had zero callers outside their own test module — every real call site already resolved through ctx.workspace_config() and core's WorkspaceConfig, and action.rs:549 needs descendants_and_self, which only exists on the core copy. reworded from "delegate" to "delete" during review since no delegation was needed. Config keeps loading, precedence, and cli_ fields$ +cli %2026-07-11T14:27 #019f268f-eb41-7389-aff1-6e7ff6cd43f0
[x] delete the redundant per-action ActionSource $SourcedAction.source stamps a per-file value onto every action: load.rs:172 sets file_path to the same PathBuf MarkdownCharter.acts_file already holds (line 183), and .project is never read anywhere. the only two readers (graph/insert.rs:120, graph/jsonld.rs:51) already iterate charters, so they read the path from the enclosing group. 3 of 5 construction sites already pass dummy PathBuf::new(). delete ActionSource and the now-dead ActionRepository, drop the field, derive provenance from the file/charter group — the more proper structure surfaced by the doctor-inventory design 2026-07-03$ +library,cli %2026-07-11T14:36 #019f2a95-9447-7069-9604-e39ef1535549
[x] delete the unused lib.rs and commands/mod.rs dead surface $merge_hashmaps, run_workspace_sql_query, and load_workspace_actions in lib.rs have zero callers outside their own module (the sql name is wrong regardless, it is SPARQL). find_plan_file and find_plan_file_for_mutation live in commands/mod.rs, not lib.rs as originally titled, and are equally dead. deleting those two removes the last production callers of mutations::resolve_reference, MatchType, and ResolvedAction, which then also become test-only and should go too — a dead-code chain the reference-resolution unification missed because it was hiding behind these two functions$ +cli %2026-07-11T14:51 #019f268f-eb44-7fcc-8d3d-bffd32ce2539
[x] make update_action use ActionUpdate and apply_updates or delete them $the struct-based update pattern in mutations.rs is good design lying dead while update_action pokes ten positional params and reimplements it inline — same call as PlanFields already proves in the argparser. done: extended ActionUpdate with scheduled_at and duration (the two fields update_action needed that it lacked), update_action now builds one ActionUpdate and calls apply_updates instead of six inline field pokes. behavior improvement as a side effect: state transitions through update now stamp completed_at like complete/cancel already do, which the old inline code did not do. verified end-to-end against a scratch workspace (priority, description, state+completed_at, scheduled_at, duration all confirmed)$ +cli %2026-07-11T14:53 #019f268f-eb45-749b-8142-8e1440b45eb8
[x] drop the vestigial opts parameter from the parse wrapper chain $get_action_list_struct threads an unused serde_json Value through tests and archive.rs — a leftover from the js-style options era. done: dropped the opts param entirely (signature is now get_action_list_struct(actions: &str)), updated the one production call site (archive.rs) and all test/example call sites (tests/lib.rs x11, tests/error_reporting.rs, tests/snapshots_from_examples.rs, examples/format_demo.rs). also deleted get_action_list — the JSON-Value-returning wrapper around it — which turned out to have zero callers anywhere, production or test$ +cli %2026-07-11T14:56 #019f268f-eb47-7f57-b8c7-2ba766b74e7a
[x] extract build_workspace_store from the query helpers $run_workspace_raw_query and its siblings duplicate the create-store, insert-metadata, load-additional-workspaces dance; one helper returning the populated store collapses them to a few lines each. RESOLVED DIFFERENTLY: the sibling this was written against, run_workspace_sql_query, was deleted in the lib.rs dead-code sweep (eb44) rather than kept and deduplicated — it had zero production callers. run_workspace_raw_query is now the only function doing this dance; extracting build_workspace_store for a single caller would be a premature abstraction, not a fix. no code change needed$ +cli %2026-07-11T14:56 #019f268f-eb48-7c81-ad4c-748ece362373
[x] sweep dead code and single-consumer compat shims $nothing links these crates except the CLI binary itself — the nvim plugin shells out — so backward-compat layers are pure carrying cost$ !2 +cli %2026-07-11T14:56 #019f268f-eb43-7994-8d0f-13c024980696
[x] route read actions through the workspace loader $collect_all_actions lists files and parses each directly — no sidecar hydration, no journal recovery, no load findings — so the most-used read command sees a different workspace than every other command: created dates missing, an interrupted batch shows torn state any loading command would have healed, and load warnings are invisible. converge on the loader; a per-file fast path survives only if the trust benchmark action proves it matters. done: collect_all_actions and collect_workspace_actions (same bug, not named in the original title but identical pattern across the multi-workspace read path) now both route through clearhead_core::load_workspace, matching every other command. --file/--charter filtering now matches MarkdownCharter.actions_file against the requested path (via charter_root-join + canonicalize) instead of reading that one file directly — the fast path is gone per the note, since no trust-benchmark action exists to justify keeping it. .completed.actions still read directly per matching file (the loader deliberately excludes them, they are an archive not live state). verified end-to-end: sidecar-hydrated created_at now shows in read/show output (previously silently absent); malformed-file recovery now warns+continues instead of hard-failing (test_read_acts_file_fails_on_malformed_input renamed/updated to assert the new behavior); multi-workspace read verified manually across two scratch workspaces$ !2 +cli %2026-07-11T15:18 #019f2ea2-6339-7f52-9fe6-0514e95e1522
[x] add a guard so the durability write seam cannot silently reopen $deny std::fs::write under src/commands via clippy disallowed-methods or a small test that greps the tree. done: a grep-guard test (tests/no_naive_writes.rs), not clippy disallowed-methods — clearhead-cli has no CI wired up at all (only clearhead-core has a workflow, and it is workflow_dispatch-only), so a clippy lint nobody runs would be a guard in name only; a cargo test guard fires on every local test run regardless. walking src/commands/ turned up two fs::write call sites the original write-durability sweep note did not name: mod.rs write_or_print (user-directed --write output) and plan.rs:899 (export --output icalendar) — both legitimately exempt for the same reason as write_or_print, both added to the EXEMPT allowlist with reasons. verified the guard actually catches a regression: added a decoy std::fs::write in src/commands/action.rs, confirmed the test fails and names the exact file:line, then removed it$ !2 +cli %2026-07-11T15:20 #019f5328-7865-7843-abd2-2c25505e47f1
[x] add close_subtree to core with tests $one function taking the action list, root id, closing state, and timestamp; returns the closed subtree for the caller to append to the completed file. collect_subtree_ids moves with it. done: clearhead_core::domain::{close_subtree, collect_subtree_ids} (re-exported at crate root), landed as free functions next to Action's impl block. close_subtree stamps completed_at for both Completed and Cancelled states, matching the CLI's current inline behavior exactly — no cancelled_at field exists yet (action-lifecycle charter, still not-started); user decided to build now and accept a follow-up tweak rather than block on it. 5 unit tests covering subtree collection (root+descendants, leaf-only), state stamping, parent detachment, and that source actions are left untouched$ +library %2026-07-11T15:25 #019f5328-b1d0-79d0-8d5c-1c20fe29d14e
[x] collapse complete_action and cancel_action onto it $both become thin callers passing Completed or Cancelled; dry-run and messaging stay in the command layer. done: extracted close_action_subtree in commands/action.rs, taking closing_state plus present/past-tense verb strings (complete/Completed, cancel/Cancelled) since simple string-suffix derivation breaks on cancel -> cancelled (double l). complete_action and cancel_action are now two-line callers. also deleted the CLI-local collect_subtree_ids (now a pure duplicate of core's) and routed delete_action onto clearhead_core::collect_subtree_ids too, so all three mutating commands share one subtree-walk implementation. verified end-to-end against a scratch workspace: complete/cancel with children stamps completed_at, detaches parent_id, moves the whole subtree to .completed.actions, dry-run and success messages byte-for-byte match the pre-refactor wording$ +cli %2026-07-11T15:27 #019f5328-b282-7a01-b519-6fd3d3fc8fd3
[x] move subtree-close semantics into core $what complete means (close the descendants, stamp completed_at, detach parents, move to the completed file) is business logic implemented inline in the CLI while core claims to own it. complete_action and cancel_action are fifty-line twins differing only in the target state. reopened 2026-07-11: originally marked complete but verification during charter review found no close_subtree function anywhere in clearhead-core, and complete_action/cancel_action in commands/action.rs are byte-for-byte unchanged from the pre-completion description — no commit, no stash, no other branch has this work$ !2 +library %2026-07-11T15:27 #019f5328-899f-7e32-ac04-3fc36b28d123
