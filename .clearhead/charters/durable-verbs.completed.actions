[x] Relocate ActionUpdate field appliers to core =relocate-updates $DONE 2026-08-15. Moved ActionUpdate/apply_updates and CharterUpdate/apply_charter_update verbatim from clearhead-cli/src/mutations.rs into new clearhead-core/src/domain/update.rs, next to close_subtree/collect_subtree_ids; re-exported at the core crate root (pub use domain::update::{...}). clearhead-cli/src/mutations.rs is now a thin re-export shim so existing call sites (action.rs, charter.rs, lib.rs) are untouched; shim removal deferred to the charter done gate when verbs reroute. The completed_at stamp moved as-is (its proper rehoming + terminal-state guard is the durable-update leaf). Pure, no behavior change: core builds + clippy clean + moved test passes; CLI builds + clippy clean + full suite 161 passed / 0 failed.$ !1 +architecture ^2026-08-15T12:24 %2026-08-15T13:31 #01a006e1-b48d-7fa3-9ef4-30f42ef64a1e
[x] Extract the locked-mutation envelope into one core seam =locked-mutation-envelope $DONE 2026-08-15. Extracted the duplicated lock -> recover_pending -> read -> plan -> render -> PendingBatch stage/commit envelope into clearhead-core/src/workspace/mutation.rs: with_locked_mutation(&WorkspaceLayout, |&layout| -> Result<(WriteSet, T)>) -> Result<T>, plus FileWrite{path,contents} and WriteSet (empty => no-op, commits nothing). Re-expressed archive_actions and close_action_subtree as pure bodies returning a WriteSet; the seam owns create_dir/lock/recover/stage/commit and preserves ordering (recover before reads) and no-op semantics (archive count==0 and close already_closed both stage nothing). Named for what it commits (file writes), not why: Plan=schedule entity, transaction=CLI batch surface, update=field edit all left reserved; seam lives in the workspace/file layer, domain stays pure. No behavior change: core strict clippy clean + 256 lib / all-binaries pass; CLI 161/0 through the complete/cancel/archive verbs; graphd builds. Keystone landed before the three verbs per the charter.$ !1 +architecture ^2026-08-15T12:24 %2026-08-15T13:43 <relocate-updates #01a006e1-ba88-7420-8d6d-1551686e9936
[x] Route the add verb through the locked core seam =durable-add $Add a pure plan_insert_action and route add through with_locked_mutation: lock, recover, read trusted state, compute the insertion-after-subtree index, stage the re-rendered active file plus its sidecar, commit one PendingBatch. Dry-run is the pure plan on an unlocked read. The CLI keeps flag-to-Action assembly and dry-run prose; the raw read_actions/insert/save_file path is removed.$ !1 +architecture ^2026-08-15T12:24 %2026-08-15T16:17 <locked-mutation-envelope #01a006e1-c0dc-78c1-b449-7c1a83afe965
[x] Route the update verb through the locked core seam =durable-update $Add a pure plan_update_action and route update through with_locked_mutation using the relocated ActionUpdate. Move the completed_at stamping and the in-place terminal-state guard into core so the rule has one home. Record an explicit decision on the relationship to the data-workflows transact planner (reuse, share, or keep separate) rather than silently duplicating it.$ !1 +architecture ^2026-08-15T12:24 %2026-08-15T16:27 <durable-add #01a006e1-c626-7b91-b500-054f254f1ee6
[x] Generalize the action selector across active and completed =generalize-selector $Generalize CloseActionSelector (active-only) into a selector that spans active and .completed.actions (delete targets both) and carries typed under-lock outcomes (not-found-under-lock, state-changed-under-lock) analogous to close's already_closed. Migrate close_action_subtree onto the generalized selector with no behavior change so the durable-action-close re-identification rule applies uniformly to close, update, and delete.$ !1 +architecture ^2026-08-15T12:24 %2026-08-15T16:33 <durable-update #01a006e1-cb7b-73a3-832e-c782c5d4ed37
[x] Route the delete verb through the locked core seam =durable-delete $Add a pure plan_delete_action_subtree and route delete through with_locked_mutation for the open-subtree case and the completed-archive single-remove case. The CLI keeps its workspace fan-out and file selection; the raw retain/save_file path is removed.$ !1 +architecture ^2026-08-15T12:24 %2026-08-15T17:22 <generalize-selector #01a006e1-d045-7f73-9013-3cf4c4e15a97
