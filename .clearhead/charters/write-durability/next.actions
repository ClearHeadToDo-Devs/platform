[x] establish write durability layer ~#019f0bc9-8931-7b05-a1be-0cc39ed83d20
    >[x] make save_domain_model set-atomic $ staged-temps + ordered renames + directory fsync + a .pending journal replayed on startup — recoverable-durability, not true atomicity; partial failure leaves pre-batch or full post-batch, never torn $+library #019f0bc9-8946-70cb-a08f-74ce8be6f2b4
        >>[x] add atomic write primitive $ single owner: write to a temp file in the same directory, fsync, then atomic rename over the target. one function every write goes through $+library #019f0bc9-8938-73b2-8c47-d359438fa494
    >[x] route all production writes through the primitive $ replace naive std::fs::write in action_files.rs, sidecar.rs, store/save.rs, and archive_charter.rs — tests can stay as-is $+library #019f0bc9-893f-7e9a-9d97-cd40f58eab92
    >[x] add staged multi-file commit seam $ the bracket reconcile will use to move an action and its sidecar snapshot together — A and B land or neither does $+library #019f0bc9-894d-7f18-9683-b6a159ca0d91
    >[x] add advisory workspace lock $ lockfile guarding ClearHead writes against concurrent ClearHead invocations only — does NOT serialize radicale; stale-lock handling (PID check / TTL) and non-blocking-acquire semantics must be defined $+cli,library #019f0bc9-8954-7740-80a5-76b0997047a7
    >[x] crash-safety tests (DONE GATE) $ simulate failure mid-write, mid-batch, and mid-staged-commit (A committed, B not); assert no torn/partial state, prior content always survives, and .pending journal replays correctly on restart — this action is the charter's completion gate $+library #019f0bc9-895b-7fda-a476-dd426127cd39
